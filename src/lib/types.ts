export type Post = {
  body: string;
  id: string;
  name: string;
  username: string;
  userId: string;
  title: string;
};

export type User = {
  id: string;
  name: string;
  username: string;
};
